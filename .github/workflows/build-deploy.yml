name: Build and Deploy Code

on: [push, pull_request]

jobs:
  job1:
    env:
      DATHOST_USER: sebastian.seitalahti@gmail.com
      DATHOST_PASSWORD: EqZMArAwMREg3CP
      DATABASE_HOSTNAME: localhost
      DATABASE_PORT: 5432
      DATABASE_PASSWORD: AKIA5AKS4RPOAAND4BOF
      DATABASE_NAME: scrimbot
      DATABASE_USERNAME: postgres
      SECRET_KEY: lkfs0rd350s934jjkl232dfd34f99klj324kllsdf09d3k3df4odsfifo3ju409dfskj3kl4df2s3lkj34243dsfsf09
      ALGORITHM: HS256
      ACCESS_TOKEN_EXPIRE_MINUTES: 30
    runs-on: ubuntu-latest
    steps:
      - name: pulling git repo
        uses: actions/checkout@v2
      - name: Install python version 3.9
        uses: actions/setup-python@v2
        with:
          python-version: "3.9"
      - name: update pip
        run: python -m pip install --upgrade pip
      - name: install dependencies
        run: pip install -r requirements.txt
      - name: test with pytest
        run: |
          pip install pytest
          pytest /home/runner/work/reenit-api/reenit-api/tests
